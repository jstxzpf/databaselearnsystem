# 数据库学习系统 - 开发环境Docker镜像
FROM python:3.11-alpine

# 设置维护者信息
LABEL maintainer="Database Learning System - Development"
LABEL description="Flask-based Database Learning System - Development Environment"

# 设置工作目录
WORKDIR /app

# 设置环境变量
ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    FLASK_APP=app.py \
    FLASK_ENV=development \
    FLASK_DEBUG=1

# 安装系统依赖
RUN apk add --no-cache \
    gcc \
    musl-dev \
    linux-headers \
    curl \
    git \
    && rm -rf /var/cache/apk/*

# 复制requirements.txt并安装Python依赖
COPY requirements.txt .

# 安装Python依赖（包括开发依赖）
RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt && \
    pip install --no-cache-dir \
        flask-debugtoolbar \
        watchdog

# 创建必要的目录
RUN mkdir -p data/explanations static/uploads logs

# 暴露端口
EXPOSE 5000

# 开发环境启动命令（通过docker-compose覆盖）
CMD ["python", "run.py"]
